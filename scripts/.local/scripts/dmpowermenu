#!/bin/sh
cmd=$(echo -e "lock\nreboot\nshutdown" | dmenu -p "menu")
[ $cmd ] && (
[ ! ${cmd##lock} ] && xsecurelock #$HOME/.local/scripts/lock.sh
[ ! ${cmd##reboot} ] && loginctl reboot
[ ! ${cmd##shutdown} ] && loginctl poweroff
)
